# PStack development progress — setup rationale, discovery test fix

Created at: 2026-09-26 15:55:20 UTC (2026-09-27 00:55:20 Asia/Tokyo)

## What changed

- `docs/guide/01-setup.md`: new "Why the setup looks like this" section. It covers three points: skills are symlinks, the model pool is user-level, and generated artifacts drift. It includes a table mapping each doctor finding to its fix. It also states that `create-verification-skill` writes a `pstack-generated-by` stamp that the doctor does not compare yet. The wording is new; none was reused from `4bc01a7` or `95ad4e2`.
- `test_harness_discovery.py` was flaky. It encoded the Claude project folder with `replace("/", "-")`, while `harness_discovery.py` (like Claude Code) replaces every non-alphanumeric character. It failed whenever the random temp dir name contained `_`. The test now uses the same encoding and passed 20 of 20 runs.

## Checks

- Script tests: 50 model policy, 13 doctor, 6 link, and 3 discovery, all passing.
- `pstack_doctor.py --project /cave/Wisdio-pstack`: exit 0, with one info finding (pool structurally valid; availability needs `ready --snapshot`).
- These checks do not prove workflow behavior. Of the workflows, only `swarm` and `interrogate` have run in a real T3 thread.
