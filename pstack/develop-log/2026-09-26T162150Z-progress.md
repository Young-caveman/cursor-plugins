# PStack development progress — manual-only workflows, test bench check, merge to main

Created at: 2026-09-26 16:21:50 UTC (2026-09-27 01:21:50 Asia/Tokyo)

## Decisions (user, 2026-09-27)

- Workflows are manual-only in every harness, the same rule Claude Code already enforces.
- Leave plan mode's read-only enforcement unverified for now.
- Merge `t3-orchestration` into `main` and push.

## What changed

- Each of the 21 workflow descriptions now opens "Run only when the user asks for this skill by name or a workflow the user started routes here." The second clause keeps poteto-mode's routing (to how, architect, swarm, and others) working. `tdd`'s "OR when the bug has an obvious cheap local test target" auto clause was removed; the bug-fix playbook already decides when to use tdd. `automate-me` writes the same rule into generated mode skills. The README and `01-setup.md` explain that in Codex and OpenCode this is an instruction, not a lock.
- New `bench_check.py` plus `test_bench_check.py` (3 tests). It lists commits past the base, uncommitted files, and PStack drift on a test bench. `--reset` asks first, then runs `reset --hard <base>` and `clean -fd`. It never uses `-x`, so the excluded links survive, and it refuses when the bench is on the base branch itself.
- `/cave/Wisdio-pstack` was already clean at `develop1-engine`. The reset had nothing to do; both link folders still hold their 47 PStack links.

## Checks

- 75 script tests pass. Pool validation, the Wisdio-pstack doctor, the bench check, and `git diff --check` pass.
- Not proven: whether Codex and OpenCode models obey the description rule. That needs a harness run: ask a plain question that matches a workflow's trigger words and check that it does not start the workflow.
