# PStack development progress

Created at: 2026-09-22 20:28:56 UTC (2026-09-23 05:28:56 Asia/Tokyo)

## Current state

- Cross-harness model-routing foundation is committed in `f3551ad` (`feat: add cross-harness PStack model routing`).
- The model policy helper executable-bit fix is committed in `179e0d8` (`fix: make model policy helper executable`).
- Harness installation guidance, PStack setup documentation, skill-name normalization, and repository handoff documentation are committed in `75e4c36` (`docs: document harness installs and project status`).
- Local `main`, `test/codex-install`, and `test/opencode-install` point to `75e4c36`. The test branches are prepared for manual checks; no test worktrees are attached.
- OpenCode child model selection and parent inheritance were exercised earlier. Codex real-harness behavior remains unverified.
- The latest installation documentation has not been validated through a real installation. The user plans to perform the installation and harness checks.
- At creation, the checkout is on `main`, clean, and three commits ahead of `origin/main`. No remote push was made for these log files.

## Next checkpoint

Update the progress log after the user tries the project-scoped Codex and OpenCode installs, capturing what succeeded, what failed, and any differences from the documented model/configuration behavior.
