# Handoff and guide sync

Committed `b97d56a`: AGENTS.md names the completed commits (`38550b1`, `178a2d7`, `bc5bb53`), the 72 passing state checks, and the workflows that still need live harness runs. `01-setup.md` now separates structural `validate` from snapshot `ready` and live resolution; `09-make-it-yours.md` uses the skill-authoring fallback instead of assuming `skill-creator` is installed. README's migration-status section landed with it. A preexisting third-person edit to the README bio line was left uncommitted on purpose.

Checks before commit: 72 script tests passed (50 model policy, 13 doctor, 6 link, 3 discovery); `model_policy.py validate`, `pstack_doctor.py --project /cave/Wisdio-pstack` (no drift), local Markdown links, and `git diff --check` passed. No harness run; the user runs those.
