# Follow-up on the `/interrogate` verdict

The completed three-reviewer verdict found two live blockers at `178a2d7`:

- T3 worktree launches were documented with `delegate_task`'s `target`, `mode`, and `clientRequestId` fields. `t3-delegation.md` and `model-routing.md` now distinguish `delegate_task` from `t3_thread_launch`, which takes `modelSelection` and has no retry key. The same conflation was fixed in `swarm` and clarified in `arena`. T3's local source and live tool schema confirm `modelSelection: {instanceId, model, options}`.
- `skill-creator` was treated as installed in every harness. Skill authoring now has a direct `SKILL.md` fallback (YAML `name` and `description`, short instructions, validation). `automate-me`, `reflect`, and `poteto-mode` use it when `skill-creator` is unavailable. OpenCode's official skills docs confirm the required frontmatter and project `.agents/skills` path.

The review's "Consider" items were addressed without adding approval stops: `interrogate` labels a result with fewer than two providers as degraded and does not claim consensus; `arena` reports when only one candidate remains. `model_policy.py validate` now rejects a pool with no `default` entry, with a regression test. `poteto-mode` identifies its entry gate as structural and requires live resolution per delegated call. For a missing project verifier, it reads `create-verification-skill` directly; shipping uses the head's verifier instructions against parent and head and records when the parent lacks the feature.

Known limit: `disable-model-invocation` is honored by Claude Code, but Codex and the installed OpenCode do not enforce it. PStack documents this; it does not claim manual-only workflow invocation in those harnesses. Plan-mode enforcement and workflow behavior beyond the exercised `swarm` and `interrogate` runs still need real T3 harness tests, which the user runs.

Checks: 72 script tests passed (50 model policy, 13 doctor, 6 link, 3 discovery); `model_policy.py validate`, `pstack_doctor.py --project /cave/Wisdio-pstack`, `git diff --check`, and skill-link resolution passed. These are state checks, not new harness behavior proof.
