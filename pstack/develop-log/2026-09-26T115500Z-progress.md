# PStack T3 migration review and onboarding docs

- Continued in Codex after the Claude coordinator hit its session limit. T3 confirms this parent thread is `codex / gpt-6-sol`; the model was already selected before this turn. A read-only onboarding-doc audit ran on the saved pool entry `opencode-deepseek-v4-1-flash-max` and completed.
- The `/interrogate` coordinator did not produce a verdict. Its Claude Sonnet 5 and OpenCode DeepSeek V4.1 Flash reviewers completed; no final report was recoverable from the Codex Luna reviewer. Their findings were checked against the current files before edits.
- Rewrote the README and guide for T3 project links, the shared v2 model pool, Codex/Claude/OpenCode invocation, and project verification skills. Corrected the pool schema description to include the required unique `id`, and removed an unsupported global-install implication.
- Fixed stale migration instructions: deleted `pstack/agents/` path, v1 arena judge role, `/deslop`, universal `/loop`, cloud-root/VM wording, Cursor `Task` worktree semantics, old reflect reviewer transcript prompts, and misleading agent-type and model-policy error text. Orchestrate briefs now carry an absolute store path because child shells do not inherit `ORCH_STORE`.
- `worktree-audit.sh` now marks merged or PR-linked worktrees for review instead of calling them safe: its session scan only sees chats whose cwd was the worktree. The cleanup playbook requires checking pinned and active T3 threads before deletion.
- Kept the new pool dropout policy and `validate` entry gate: the former follows `t3-delegation.md` (continue with N−1), and targets are checked with `resolve` before delegation. The review's suggestions to restore old per-role runner counts and unconditional review pauses conflict with the v2 pool and the user's preference to avoid needless stops.
- Checks: 71 script tests passed (49 model policy, 13 doctor, 6 link, 3 discovery); `bash -n` and `git diff --check` passed; local Markdown links resolved except the existing `](url)` placeholder in `why/references/synthesizer-prompt.md`; `pstack_doctor.py --project /cave/Wisdio-pstack` reported no drift. No post-edit harness run; the user runs those tests.
