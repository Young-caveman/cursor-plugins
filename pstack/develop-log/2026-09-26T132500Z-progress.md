# README voice rewrite

The README no longer speaks in poteto's first-person voice. Her origin note is one sentence in third person ("she wrote it..."), the manifesto is cut to three short claims, and the body uses the guide's neutral register. The lead framing is now T3 Code: `orchestrator_capabilities`, `delegate_task`/`t3_thread_launch`, and the shared model pool. The preexisting bio edit is folded in.

Left alone on purpose: user-voice example prompts ("show me", "I'm going to bed"), the Comment Sicko persona file, and the guide, which was already neutral.

Checks: 72 script tests, `pstack_doctor.py --project /cave/Wisdio-pstack`, local links, `git diff --check` passed. One flaky `test_pstack_link` temp-dir failure on first run passed on rerun; not investigated.
